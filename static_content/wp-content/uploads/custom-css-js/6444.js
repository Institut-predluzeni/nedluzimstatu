/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: Jun 30 2023 | 15:09:28 */
// taken from here - https://gist.github.com/Garconis/6d4599f79725a292ca56f869bd2e6ab3 and customize for different header heigt
jQuery(function($) {
	window.et_pb_smooth_scroll = function($target, $top_section, speed, easing) {

		var $window_width = $(window).width();

		$menu_offset = -1;
		
		if ($window_width <= 981) {
			var headerHeight = 144;
			if  ($("body").hasClass("page-id-6100")) {
				var headerHeight = 88;
			}
		}
		else {
			var headerHeight = 154;
			if  ($("body").hasClass("page-id-6100")) {
				var headerHeight = 100;
			}			
		}

		if ($('#wpadminbar').length) {
			$menu_offset += $('#wpadminbar').outerHeight() + headerHeight;
		}
		else {
			$menu_offset += headerHeight;
		}

		//fix sidenav scroll to top
		if ($top_section) {
			$scroll_position = 0;
		}
		else {
			$scroll_position = $target.offset().top - $menu_offset;
		}

		// set swing (animate's scrollTop default) as default value
		if (typeof easing === 'undefined') {
			easing = 'swing';
		}

		$('html, body').animate({ scrollTop: $scroll_position }, speed, easing);

	}
});